apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: investra

spec:

  type: ClusterIP

  selector:
    app: order-service

  ports:

    - name: http

      protocol: TCP

      port: 8083

      targetPort: 8083